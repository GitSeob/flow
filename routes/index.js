const express = require('express');
const router = express.Router();

router.get('/add/:extension_name', (req, res) => {

})

router.get('/delete/:extension_name', (req, res) => {

})

router.get('/check/:extension_name', (req, res) => {

})

module.exports = router;
